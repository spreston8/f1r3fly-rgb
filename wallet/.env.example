# RGB Integration Test Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   # Then edit .env with your mnemonic

# REQUIRED: Your funded signet wallet mnemonic (12 words)
# Wallet should have at least 50,000 sats
TEST_MNEMONIC="your twelve word mnemonic phrase goes here"

# OPTIONAL: Confirmation timeout in seconds (default: 900 = 15 minutes)
# Increase if blocks are slow
TEST_TIMEOUT=900

# OPTIONAL: Fee rate in sat/vB (default: 20)
# Lower = cheaper but slower (e.g., 10 sat/vB)
# Higher = faster but more expensive (e.g., 30 sat/vB)
TEST_FEE_RATE=20

# OPTIONAL: Rust log level for debugging
# Recommended: Only show debug logs for test and wallet, suppress all HTTP/TLS noise
RUST_LOG=info,rgb_transfer_balance_test=debug,wallet=debug,hyper=warn,reqwest=warn,rustls=warn

# Alternative log configurations:
# - Show everything: RUST_LOG=debug  (⚠️ VERY verbose, includes HTTP/TLS handshake details)
# - Quiet mode: RUST_LOG=info  (only important messages)
# - Trace wallet only: RUST_LOG=info,wallet=trace,hyper=warn,reqwest=warn,rustls=warn
# - Debug RGB operations: RUST_LOG=info,wallet::wallet::rgb_transfer_ops=debug,wallet::wallet::balance_ops=debug,hyper=warn,reqwest=warn,rustls=warn

# F1r3fly-RGB-Dev-Kit

## Setup

Clone the project:
```bash
git clone <repository-url>
cd f1r3fly-rgb-dev-kit
```

Initialize submodules:
```bash
git submodule update --init --recursive
```

Update submodules:
```bash
git submodule update --remote
```